<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="ru.keepitlock.featurecourses.presentation.CoursesFragment"
        android:label="Главная"
        tools:layout="@layout/fragment_courses" />

    <fragment
        android:id="@+id/nav_favorites"
        android:name="ru.keepitlock.featurefavorites.presentation.FavoritesFragment"
        android:label="Избранное"
        tools:layout="@layout/fragment_favorites" />

    <fragment
        android:id="@+id/nav_profile"
        android:name="ru.keepitlock.featureaccount.presentation.AccountFragment"
        android:label="Профиль"
        tools:layout="@layout/fragment_account" />

</navigation>